.MODEL SMALL
.STACK 1000
.DATA

MSGA DB 13,10,"NUMERO 1: ","$"
MSGB DB 13,10,"NUMERO 2:","$"
MSGS DB 13,10,"SUMA = ","$"
MSGR DB 13,10,"RESTA = ","$"
MSGM DB 13,10,"MULTIPLICACION = ","$"
MSGD DB 13,10,"DIVISION = ","$"
MSGMM DB 13,10,"MODULO = ","$"

NUM1 db ?
NUM2 db ?
NUM3 db ?

.CODE

MAIN PROC FAR

MOV AX, @DATA
MOV DS, AX


LEA DX, MSGA ;numero 1
MOV AH, 09h
INT 21h

MOV AH, 01
INT 21H
SUB AL, '0'

MOV BL, AL

MOV AH, 01
INT 21H
SUB AL, '0'

MOV CL, AL


LEA DX, MSGB ;numero 2
MOV AH, 09h
INT 21h

MOV AH, 01
INT 21H
SUB AL, '0'

MOV DL, AL

MOV AH, 01
INT 21H
SUB AL, '0'

MOV DH, AL

; add
MOV AL, CL
MOV AH, BL
ADD AL, DH
AAA 
ADD AH, DL

MOV NUM1, AL
ADD NUM1, '0'

;Verificando si el numero de 9 digitos
CMP AH, 9
JLE NOT_3DIGIT 

IS_3DIGIT:
MOV AL, AH   
SUB AH, AH   
ADD AL, 0    
AAA          
ADD AH, 0    
MOV NUM2, AL
MOV NUM3, AH
ADD NUM2, '0'
ADD NUM3, '0'

LEA DX, MSGS ; IMPRIMIR SUMA
MOV AH, 09h
INT 21h
MOV DL, NUM3
MOV AH, 02H
INT 21h
MOV DL, NUM2
MOV AH, 02H
INT 21h
JMP PRINT_LASTDIGIT


NOT_3DIGIT:    
MOV NUM2, AH
ADD NUM2, '0'
; output sum
LEA DX, MSGS
MOV AH, 09h
INT 21h
MOV DL, NUM2
MOV AH, 02H
INT 21h

PRINT_LASTDIGIT:
MOV DL, NUM1
MOV AH, 02H
INT 21h

EXIT:
MOV AH, 4Ch
INT 21h

MAIN ENDP
END MAIN